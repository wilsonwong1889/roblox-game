-- TycoonA Model Structure
-- This file defines the basic tycoon model for Week 1
-- In a real implementation, this would be created in Roblox Studio
-- For now, this serves as documentation for the required structure

-- TycoonA (Model)
-- ├── BaseFloor (Part) - 50x50x2 studs, anchored
-- ├── DropperSocket (Part) - 4x4x1 studs, positioned at (0, 1, -20)
-- ├── CollectorSocket (Part) - 4x4x1 studs, positioned at (0, 1, 0)
-- ├── DoorSocket (Part) - 4x4x1 studs, positioned at (0, 1, 20)
-- ├── OwnerSign (Part) - 8x4x0.2 studs, positioned at (0, 5, -25)
-- │   └── SurfaceGui
-- │       └── TextLabel - "Tycoon A - Unclaimed"
-- └── TycoonASpawn (SpawnLocation) - positioned at (0, 2, -30)

-- TycoonB (Model)
-- ├── BaseFloor (Part) - 50x50x2 studs, anchored
-- ├── DropperSocket (Part) - 4x4x1 studs, positioned at (0, 1, -20)
-- ├── CollectorSocket (Part) - 4x4x1 studs, positioned at (0, 1, 0)
-- ├── DoorSocket (Part) - 4x4x1 studs, positioned at (0, 1, 20)
-- ├── OwnerSign (Part) - 8x4x0.2 studs, positioned at (0, 5, -25)
-- │   └── SurfaceGui
-- │       └── TextLabel - "Tycoon B - Unclaimed"
-- └── TycoonBSpawn (SpawnLocation) - positioned at (0, 2, -30)

-- Position TycoonA at (-100, 0, 0) in Workspace
-- Position TycoonB at (100, 0, 0) in Workspace

print("Tycoon models should be created in Roblox Studio with the above structure")
